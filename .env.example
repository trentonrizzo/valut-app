# Supabase (frontend)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Cloudflare R2 (server-side only) — signed-url uses R2_BUCKET
R2_BUCKET_NAME=vault-storage
R2_BUCKET=vault-storage
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ENDPOINT=
# Optional legacy; uploads store object keys only. Media reads use GET /api/media/signed-url
# R2_PUBLIC_URL=
